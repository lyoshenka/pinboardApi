- rate limiting for 429 response codes
- oauth (when pinboard supports it)
- better error handling
- better docs for how to use
- remove test stuff from git
- validate params passed for each method, or at least that the param is supported by that method
